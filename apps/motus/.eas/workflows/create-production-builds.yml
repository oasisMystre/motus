name: Create Production Builds

on:
  push: 
    branches: ['main']

jobs:
  build_ios:
    type: build
    params:
      platform: ios
      profile: preview
  # build_android:
  #   type: build 
  #   profile: preview
  #   params:
  #     platform: android
